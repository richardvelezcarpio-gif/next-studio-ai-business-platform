import { useState } from "react";
import { en, booking as bookingEn } from "../locales/en";
import { es, booking as bookingEs } from "../locales/es";
export function BookDemoPage({locale}:{locale:"en"|"es"}){const t=locale==="en"?bookingEn:bookingEs,[done,setDone]=useState(false);const fields=[[t.name,"text"],[t.business,"text"],[t.email,"email"],[t.phone,"tel"],[t.date,"date"],[t.time,"time"],[t.type,"text"]] as const;return <section className="section"><div className="shell"><form className="premium-card mx-auto max-w-2xl p-7 sm:p-10" onSubmit={e=>{e.preventDefault();setDone(true)}}><p className="eyebrow">NEXT STUDIO</p><h1 className="mt-3 text-4xl font-black">{t.title}</h1><p className="mt-3 text-slate-500">{t.subtitle}</p><div className="mt-7 grid gap-4 sm:grid-cols-2">{fields.map(([label,type])=><label key={label} className="text-sm font-bold">{label}<input required type={type} className="mt-2 w-full rounded-xl border border-blue-100 p-3"/></label>)}</div><label className="mt-4 block text-sm font-bold">{t.message}<textarea className="mt-2 w-full rounded-xl border border-blue-100 p-3" rows={4}/></label>{done&&<p className="mt-4 rounded-xl bg-emerald-50 p-3 text-sm font-bold text-emerald-700">{t.success}</p>}<button type="submit" className="primary mt-6">{t.submit}</button></form></div></section>}
