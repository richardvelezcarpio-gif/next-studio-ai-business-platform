import {useLocation,useNavigate}from"react-router-dom";import{localizePath}from"../../i18n/routes";export function LanguageSwitcher(){const l=useLocation(),n=useNavigate(),en=l.pathname.startsWith("/en");function toggle(){const target=en?"es":"en";localStorage.setItem("next-studio-language",target);n(`${localizePath(l.pathname,target)}${l.search}${l.hash}`)}return <button onClick={toggle} className="rounded-lg border border-blue-200 px-3 py-2 text-xs font-black text-brand">{en?"ES":"EN"}</button>}
