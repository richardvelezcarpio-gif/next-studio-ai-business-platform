import {Link,NavLink}from"react-router-dom";import {Logo}from"../common/Logo";import {LanguageSwitcher}from"../common/LanguageSwitcher";import {en}from"../../locales/en";import {es}from"../../locales/es";
export function PublicHeader({locale}:{locale:"en"|"es"}){const t=locale==="en"?en:es,slug=(en:string,es:string)=>locale==="en"?en:es;const nav=[["templates","plantillas",t.nav.templates],["features","funciones",t.nav.features],["pricing","precios",t.nav.pricing]];const links=<>{nav.map(([enPath,esPath,label])=><NavLink key={enPath} to={`/${locale}/${slug(enPath,esPath)}`} className={({isActive})=>isActive?"text-brand":"hover:text-brand"}>{label}</NavLink>)}</>;return <header className="sticky top-0 z-30 border-b border-blue-100 bg-white/90 backdrop-blur"><div className="shell flex min-h-[72px] items-center justify-between gap-4"><Logo locale={locale}/><nav className="hidden gap-5 text-sm font-bold text-slate-600 md:flex"><Link to={`/${locale}/demo`}>{t.nav.demo}</Link>{links}</nav><div className="flex items-center gap-2"><LanguageSwitcher/><Link className="primary text-sm" to={`/${locale}/${slug("pricing","precios")}`}>{t.nav.buy}</Link></div></div><nav aria-label="Marketing" className="shell flex gap-5 overflow-x-auto border-t border-blue-50 py-3 text-sm font-bold text-slate-600 md:hidden"><Link className="shrink-0" to={`/${locale}/demo`}>{t.nav.demo}</Link>{links}</nav></header>}
