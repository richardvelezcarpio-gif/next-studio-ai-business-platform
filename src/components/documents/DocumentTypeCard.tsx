import {FileText,ReceiptText,ScrollText,ArrowRight}from"lucide-react";import{Link}from"react-router-dom";import{en}from"../../locales/en";import{es}from"../../locales/es";const icons=[ReceiptText,FileText,ScrollText];export function DocumentTypeCard({title,index,to,start,demo,locale="en"}:{title:string;index:number;to:string;start:string;demo:string;locale?:"en"|"es"}){const Icon=icons[index],t=locale==="en"?en:es;return <article className="card overflow-hidden"><div className={`h-36 bg-gradient-to-br ${index===0?"from-blue-100 to-white":index===1?"from-sky-100 to-blue-50":"from-slate-200 to-blue-100"} p-6`}><Icon className="text-brand" size={42}/></div><div className="p-6"><span className="text-xs font-black text-brand">{demo}</span><h3 className="mt-4 text-xl font-black text-navy">{title}</h3><p className="mt-2 text-sm leading-6 text-slate-500">{t.home.subtitle}</p><Link className="mt-5 inline-flex items-center gap-2 font-black text-brand" to={to}>{start}<ArrowRight size={16}/></Link></div></article>}
