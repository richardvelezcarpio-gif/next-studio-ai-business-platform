import { readFile } from "node:fs/promises";
const files=["src/locales/en.ts","src/locales/es.ts"];const contents=await Promise.all(files.map(file=>readFile(file,"utf8")));const keys=contents.map(source=>[...source.matchAll(/(?:[,{])\s*([A-Za-z][A-Za-z0-9]*)\s*:/g)].map(match=>match[1]).sort());const only=(left,right)=>left.filter((key,index)=>left.indexOf(key)===index&&!right.includes(key));const missingEs=only(keys[0],keys[1]),missingEn=only(keys[1],keys[0]);const empty=contents.flatMap((source,index)=>[...source.matchAll(/:\s*(?:""|undefined|null)(?=[,}])/g)].map(()=>files[index]));if(missingEs.length||missingEn.length||empty.length){if(missingEs.length)console.error(`Missing in Spanish: ${missingEs.join(", ")}`);if(missingEn.length)console.error(`Missing in English: ${missingEn.join(", ")}`);if(empty.length)console.error(`Empty locale values: ${empty.join(", ")}`);process.exit(1)}console.log("Locale structure audit passed.");
